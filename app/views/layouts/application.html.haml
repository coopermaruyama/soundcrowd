!!! 5
%html
	%head
		%title= "Soundcrowd"
		= stylesheet_link_tag    "application", :media => "all"
		= javascript_include_tag "application"
		= csrf_meta_tags
	%body
		%header
			= link_to logo, "/"
			%nav
				%a.signup= link_to "Sign Up", signup_path 
		.clr
		.main
			%p.notice= notice
			%p.alert= alert
			= yield
			.clr
			%br
			= link_to_function "« Back", "history.back()", :class => "back"
			= debug(params) if Rails.env.development?
