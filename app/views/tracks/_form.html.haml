= form_for @production, :html => {:multipart => true} do |f|
	= f.label :title
	= f.text_field :title
	= f.fields_for :versions do |version|
		= version.label :audio
		= version.file_field :audio
	= f.submit
