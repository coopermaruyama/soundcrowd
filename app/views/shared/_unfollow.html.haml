= form_for(current_user.relationships.find_by_followed_id(@user),html: {method: :delete, class: "follow"}, remote: true) do |f|
	= button_tag(:type => "submit", class: "btn-action lighten active") do
		= image_tag('icons/18px/smiley.png',:class => "icon follow")
		= "unfollow"