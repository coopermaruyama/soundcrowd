#follow_form
	- if user_signed_in?
		- if current_user.following_production?(production)
			= render "shared/unfollow_production", :production => production
		- else
			= render "shared/follow_production", :production => production