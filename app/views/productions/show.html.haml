.project-head
	= avatar(@production.creator)
	.project-title
		%h1.title= @production.title
		.clr
		%span.creator-info
			= "By:" 
			= link_to @production.creator.username , @production.creator

.clr

#main
	.project-left
		.label-left
			%h2 ..
			%h2= @production.versions.length
		.label-right
			%h2
				%span.label
					BPM
			%h2
				%span.label
					versions
			%h2
				%span.label
					followers
	.project-main
		= render :partial => "production" 
.clr