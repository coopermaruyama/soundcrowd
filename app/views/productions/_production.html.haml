- @production.versions.each do |version|
	.version
		= avatar(version.user)
		.version-info
			%h3.version-title= version.title
			%span.version-user
				By: 
				= link_to version.user.username, version.user
		.clr
		= render :partial => "player", :locals => { :version => version }