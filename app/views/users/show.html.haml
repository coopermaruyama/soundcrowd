- content_for :head do
	= stylesheet_link_tag "users"
= avatar(@user)
%h1.username= @user.username
%span.followers= link_to "#{@user.followers.count} Followers", followers_user_path(@user)
%span.followers= link_to "#{@user.followed_users.count} Following", following_user_path(@user)
.clr
