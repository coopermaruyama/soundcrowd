= form_for(current_user.relationships.build(followed_id: user.id), :html => { class: "follow" }, remote: true) do |f|
	= f.hidden_field :followed_id
	%button{:type => "submit", class: "btn-action lighten"}
		= image_tag('icons/18px/smiley.png',:class => "icon follow")
		= "follow"